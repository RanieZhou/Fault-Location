# 配网故障定位 Agent — 全栈实施计划（方案D）

## 一、现有代码审查结果

### ✅ 正确无误的部分

| 文件 | 状态 | 说明 |
|-----|------|------|
| `common.py` | ✅ 正确 | 名称解析逻辑正确，正则完整，别名处理了"松平线"typo |
| `build_topology.py` | ✅ 正确 | 树形构建逻辑正确，BFS编号、祖先链校验均无误 |
| `calibrate_load.py` | ✅ 正确 | KCL反推本地负荷逻辑正确，负值截断、负值标记均合理 |
| `draw_topology.py` | ✅ 正确 | 拓扑可视化正确，主干/支路布局算法合理 |
| `validate_hierarchy.py` | ✅ 正确 | 祖先关系校验逻辑正确 |
| `locate_fault.py` | ✅ 正确 | 矩阵法/报警前沿算法实现正确 |
| `build_feeder_model.m` | ✅ 正确 | 参数转换公式（正/零序→自感/互感）正确 |
| `generate_fault_dataset.m` | ✅ 正确 | 故障场景枚举、parsim并行、数据提取逻辑正确 |
| `validate_hl_baseline.m` | ✅ 正确 | 基线仿真校验逻辑正确 |
| `validate_sp_baseline.m` | ✅ 正确 | 同上 |
| `run_build_hl.m` / `run_build_sp.m` | ✅ 正确 | 入口脚本正确 |

### ⚠️ 发现的问题（共5个）

#### 问题1：`estimate_line_params.py` — 支线/干线判断标准粗糙
```python
# 当前逻辑（第114行）：只看 path 长度是否为1来判断是否是干线
is_trunk = len(child) == 1  # 主干/主干补杆,没有小数点分支
```
**问题**：编号 `51+1`、`66+1` 等补杆节点 `len(path)==1`，判为干线是对的；但像 `27.3`、`27.6.2` 等二级以上节点也有可能是主干延伸而非支线分叉。当前逻辑对此无法区分。

**影响**：线路参数估算存在一定误差（支线R偏大），但因参数本身就是粗估，工程影响有限。**暂不修改，记录即可。**

---

#### 问题2：`estimate_line_params.py` — `fitz` 库读取PDF长度依赖不稳
```python
m = re.search(r'总长度[:：]\s*([\d.]+)\s*km', text)
if not m:
    raise RuntimeError(f'{line} 图纸里没找到总长度标注')
```
**问题**：PDF解析依赖字体/编码，如果PDF是扫描件或含矢量图形的总长度，正则可能匹配不到。此时程序直接抛出异常，没有fallback。

**修复方案**：添加 fallback：若从PDF未找到，尝试从 `status_tags.txt` 或手工配置文件读取长度，或给出提示让用户手动填写。**列为需修复。**

---

#### 问题3：`locate_fault.py` — 事件分组算法对分叉拓扑处理欠佳
```python
# 当前逻辑：同一线路、15秒内的报警点合并为一个事件
if cur and p['line'] == cur[-1]['line'] and abs((p['time'] - cur[-1]['time']).total_seconds()) <= 15:
```
**问题**：15秒时间窗是固定的，对于大型线路或通信延迟较大的场景可能不够；同时，`cur[-1]` 只和最后一条记录比较时间，若第1条和第3条时间差大于15s、第1和第2、第2和第3各小于15s，则3条会被分成两个事件（而实际是同一次故障）。

**修复方案**：改为"同一事件内任意两条时间差不超过阈值"的滑动窗口，并将阈值做成可配置参数。**列为需修复。**

---

#### 问题4：`build_feeder_model.m` — 负荷封顶值 `400kVA` 偏保守
```matlab
S = min(S, 400e3);  % 封顶,避免个别KCL反推噪声导致的离群大值
```
**问题**：`node_loads.csv` 中 SP-0008（27.3.88.2.2.9）节点本地负荷合计约 **1250 kVA**（三相），被封顶到400kVA（单相），实际负荷被严重低估，会影响仿真电流精度。

**修复方案**：将封顶值从 400kVA 提高到至少 1500kVA，或者改成"三相合计"的封顶逻辑。**列为需修复（影响仿真质量）。**

---

#### 问题5：`sim_dataset_SP.csv` — 仿真数据量严重不足
**现状**：`sim_dataset_SP.csv` 只有29行数据，仅覆盖了**松坪线前2个节点**（SP-0001, SP-0002），缺少：
- 其余17个SP节点的仿真数据
- 全部25个HL节点的仿真数据
- 不同故障电阻值（仅用了1和5Ω，缺少10、20、50Ω的高阻接地场景）

**修复方案**：在修复问题4后，重跑完整的仿真数据生成。**列为需补充（影响AI模型训练质量）。**

---

### 📊 已有输出文件质量评估

| 输出文件 | 质量 | 说明 |
|---------|------|------|
| `nodes.csv` | ✅ 好 | 44个监测点（SP:19, HL:25），编号正确 |
| `edges.csv` | ✅ 好 | 44条边，拓扑连接正确 |
| `name_mapping.csv` | ✅ 好 | 46条映射（含81号杆大/小两侧），能正确处理typo |
| `edges_with_params.csv` | 🔶 可用 | 参数为粗估值，支线/干线判断略粗，满足仿真使用 |
| `node_loads.csv` | 🔶 可用 | KCL反推值，19/44个节点有负值截断标记，精度有限 |
| `fault_location_report.csv` | ✅ 好 | 15次历史故障均有定位结论 |
| `sim_dataset_SP.csv` | ❌ 不足 | 只有前2节点×少数故障类型，需大幅扩充 |

---

## 二、方案D全栈实施计划

> 目标：1个月内交付一个可向电力公司演示的完整产品原型。

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Web 前端（Vue3 / 原生JS）              │
│  拓扑可视化 | 故障查询 | Agent对话 | 历史事件 | 报表      │
└─────────────────────────┬───────────────────────────────┘
                          │ REST API / WebSocket
┌─────────────────────────┴───────────────────────────────┐
│                   FastAPI 后端                           │
│  ┌───────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ 核心引擎   │  │  LLM Agent   │  │   数据管理服务    │  │
│  │ (core/)   │  │  (agent/)    │  │   (data_svc/)    │  │
│  └───────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────┬───────────────────────────────┘
                          │
┌─────────────────────────┴───────────────────────────────┐
│                    数据层                                │
│  SQLite/JSON 历史事件库  | CSV 静态数据  | MATLAB 仿真   │
└─────────────────────────────────────────────────────────┘
```

---

### Phase 1：核心引擎重构（第1-2周）

**目标**：将分散脚本整合为干净的Python包，修复已知Bug，补充仿真数据。

#### 1.1 目录结构重组

```
d:\Desktop\故障定位\
├── core/                          # 核心Python包
│   ├── __init__.py
│   ├── config.py                  # 路径、常量、可配参数
│   ├── parser.py                  # 从 common.py 迁移+增强
│   ├── topology.py                # 从 build_topology.py 重构
│   ├── data_loader.py             # 统一数据加载（Excel/CSV）
│   ├── fault_locator.py           # 从 locate_fault.py 重构+修复Bug3
│   ├── electrical_analyzer.py     # 新增：电气量分析（电压降/电流比值）
│   └── models.py                  # Pydantic数据模型定义
├── agent/                         # LLM Agent包
│   ├── __init__.py
│   ├── tools.py                   # Agent工具函数（调用core层）
│   ├── prompts.py                 # 系统提示词（中文，电力专业）
│   └── graph.py                   # LangGraph工作流定义
├── web/                           # Web后端+前端
│   ├── app.py                     # FastAPI应用入口
│   ├── routers/                   # API路由
│   │   ├── topology.py
│   │   ├── fault.py
│   │   └── agent.py
│   └── static/                    # 前端文件
├── scripts/                       # 原有脚本（保留，不删除）
├── data/                          # 数据（不变）
├── output/                        # 静态输出（不变）
├── requirements.txt               # Python依赖
└── README.md
```

#### 1.2 需要修复的Bug（本阶段）

- **[Bug3修复]** `fault_locator.py`：事件分组改为滑动窗口，阈值可配置
- **[Bug4修复]** `build_feeder_model.m`：负荷封顶从400kVA改为3000kVA（三相合计）
- **[Bug2修复]** `estimate_line_params.py`：添加PDF解析失败的fallback配置

#### 1.3 补充仿真数据（本阶段尾）

运行完整的MATLAB仿真，覆盖：
- SP线路全部19个节点 × HL线路全部25个节点
- 故障类型：4（单相A接地）、5（BC两相）、6（AC两相）、10（三相）
- 故障电阻：1Ω、5Ω、10Ω、20Ω（含高阻接地场景）
- 预计生成：44节点 × 4类型 × 4电阻 = **704行**仿真数据（每行含所有监测点读数）

#### 1.4 `electrical_analyzer.py`（新增核心能力）

利用正常数据+故障数据，新增以下分析功能：

| 分析功能 | 原理 | 解决的问题 |
|---------|------|----------|
| **电压骤降比率** | 故障时各点电压/正常电压 | 辅助区分故障点位置（越近电压下降越多） |
| **电流突增比率** | 故障电流/正常电流 | 判断短路类型和严重程度 |
| **三相不平衡度** | 最大偏差/平均值 | 区分接地故障和相间短路 |
| **历史模式匹配** | 欧氏距离匹配历史故障指纹 | 有仿真数据后提供"最相似历史案例" |

---

### Phase 2：LLM Agent开发（第3周）

**目标**：基于DeepSeek/Qwen API，构建能自然语言交互的故障分析Agent。

#### 2.1 Agent工具清单

```python
# agent/tools.py 中定义的工具（LLM可以调用）

@tool
def query_topology(line: str) -> dict:
    """查询指定线路的拓扑结构，返回节点列表和连接关系"""

@tool
def locate_fault_from_alarms(line: str, alarm_points: list[str]) -> dict:
    """根据报警的监测点列表，用矩阵法确定故障区段"""

@tool
def analyze_electrical_data(line: str, event_time: str) -> dict:
    """分析某次故障事件的电气量特征（电压降、电流比值、不平衡度）"""

@tool
def search_similar_history(alarm_pattern: dict) -> list:
    """在历史故障事件库中搜索与当前报警模式最相似的历史案例"""

@tool  
def get_node_info(node_id: str) -> dict:
    """获取指定节点的详细信息：地理位置、负荷大小、历史故障次数"""

@tool
def generate_dispatch_suggestion(fault_section: str, fault_type: str) -> str:
    """根据定位结果生成标准化的派工建议"""
```

#### 2.2 Agent对话示例（目标效果）

```
运维人员: 火龙线18号杆和18.25号杆刚才同时报了短路B相
         
Agent: 我来分析这次故障。
       
       [调用 locate_fault_from_alarms]
       根据拓扑分析，18号杆和18.25号杆构成祖先-子孙关系。
       18.25是报警前沿节点（其下游的18.35未报警）。
       
       ▶ 故障区段：18.25号杆 → 18.35号杆之间
       ▶ 故障类型：B相短路
       ▶ 置信度：高（单一候选区段）
       
       [调用 search_similar_history]
       历史上2024年3月27日该线路18~18.25杆发生过类似三相短路，
       最终查明原因为18.3杆处树竹挂线。
       
       ▶ 建议：派巡线人员从18.25号杆出发向18.35方向检查线路，
              重点关注树障、绝缘子放电迹象。
              预计巡查距离约5.4km（估算区段长度）。
```

#### 2.3 提示词设计要点

- 系统角色：你是配网故障定位专家助手
- 强制要求：每次分析必须先调用工具，不允许凭空猜测
- 输出格式：结构化（故障区段 / 故障类型 / 置信度 / 巡线建议）
- 语言风格：专业但易懂，适合一线运维人员阅读

---

### Phase 3：Web系统与产品化（第4周）

**目标**：构建可向电力公司演示的完整Web界面。

#### 3.1 核心页面

| 页面 | 功能 |
|-----|------|
| **实时监控台** | 拓扑图（D3.js交互），节点状态实时（模拟），报警闪烁提示 |
| **故障分析** | 输入报警点 → 一键定位 → 结果高亮显示在拓扑图上 |
| **Agent对话** | 类ChatGPT界面，自然语言描述故障，Agent自动分析 |
| **历史事件** | 所有历史故障的时间线，支持筛选/搜索 |
| **系统配置** | LLM选择（DeepSeek/Qwen）、线路参数、阈值调整 |

#### 3.2 技术选型

| 层次 | 技术选型 | 理由 |
|-----|---------|------|
| **后端** | Python FastAPI | 异步高性能，已有Python代码复用方便 |
| **前端** | Vue3 + D3.js（拓扑图）| 成熟生态，D3适合电力拓扑可视化 |
| **数据库** | SQLite | 轻量，适合单机部署，方便迁移 |
| **LLM调用** | LangChain + 自定义OpenAI-compatible | DeepSeek/Qwen均支持OpenAI接口 |
| **部署** | 单机Docker 或直接Python运行 | 电力公司内网环境，简单部署优先 |

---

## 三、详细周计划

### 第1周（9月4日-10日）：核心包重构 + Bug修复

| 天 | 任务 |
|----|------|
| 1-2 | 创建目录结构，编写 `core/config.py`、`core/models.py`、`core/parser.py` |
| 3-4 | 重构 `core/topology.py`（合并build_topology.py），修复Bug3（事件分组） |
| 4-5 | 重构 `core/fault_locator.py`，添加 `core/electrical_analyzer.py` |
| 5 | 编写单元测试，验证15次历史事件全部通过 |

### 第2周（9月11日-17日）：仿真扩充 + 数据层

| 天 | 任务 |
|----|------|
| 1-2 | 修复Bug4（MATLAB负荷封顶），重建 feeder_SP.slx 和 feeder_HL.slx |
| 2-4 | 运行完整仿真（44节点×4类型×4电阻），生成 sim_dataset_SP.csv 和 sim_dataset_HL.csv |
| 4-5 | 构建 `core/data_loader.py`，整合历史数据+仿真数据，搭建SQLite库 |

### 第3周（9月18日-24日）：LLM Agent + API

| 天 | 任务 |
|----|------|
| 1-2 | 编写 `agent/tools.py`（6个工具函数），连接DeepSeek/Qwen API |
| 2-3 | 编写 `agent/prompts.py`（系统提示词），搭建 LangGraph 工作流 |
| 3-4 | 构建 FastAPI 后端（`web/app.py` + 路由），测试API接口 |
| 4-5 | 端到端测试：用历史15次事件验证Agent分析结果的准确性 |

### 第4周（9月25日-10月2日）：Web前端 + 产品化

| 天 | 任务 |
|----|------|
| 1-2 | 搭建Vue3项目，实现D3.js交互拓扑图（含故障区段高亮） |
| 2-3 | 实现故障分析页面和Agent对话界面 |
| 3-4 | 实现历史事件页面、配置页面、整体UI美化 |
| 4-5 | 联调测试，准备演示，编写简单使用说明 |

---

## 四、立即开始的第一步

请确认以下事项后我开始实施：

> [!IMPORTANT]
> **需要你确认的3件事**：
> 1. **LLM API配置**：你用DeepSeek还是Qwen？需要API Key（我不会记录，只写入本地 `.env` 文件）
> 2. **MATLAB重跑时机**：Bug4的MATLAB修复需要你在本机运行，我来准备脚本。你什么时候方便跑？
> 3. **是否有历史数据的接地方式信息**？（松坪线/火龙线是中性点不接地还是小电阻接地？这影响仿真中零序参数的重要程度）

一旦确认，我立即开始 **Phase 1 第1天的工作**：创建目录结构，编写 `core/config.py`、`core/models.py` 和 `core/parser.py`。
